import { CoutingSortPipe } from './couting-sort.pipe';

describe('CoutingSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CoutingSortPipe();
    expect(pipe).toBeTruthy();
  });
});
